package org.citadel.controllers.local.logic;

import org.citadel.controllers.local.LocalOperationControllerBuilder;

public class EndState extends State {

    EndState(LocalOperationControllerBuilder builder) {
        super(builder);
    }
}
